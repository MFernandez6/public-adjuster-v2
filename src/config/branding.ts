/** Public-facing business name (site copy, JSON-LD, email subjects). */
export const legalEntityName = "BLACKLINE PUBLIC ADJUSTERS LLC";

/** Short nickname (CTAs, mid-copy). */
export const brandShortName = "Blackline PA";

/** Full trade name without entity suffix (sentences, titles). */
export const tradeName = "Blackline Public Adjusters";
